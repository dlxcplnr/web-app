stages:
  - deploy

deploy_to_server:
  stage: deploy
  script:
    - cd /home/gitlab-admin/my-web-app || cd ~/my-web-app
    - git pull origin main
    - sudo docker compose down
    - sudo docker compose up -d --build
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
  tags:
    - docker-deploy